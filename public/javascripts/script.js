// Generated by CoffeeScript 1.7.1
(function() {
  var css, head, size, style, _ref;
  size = localStorage['thumb-size'];
  if (!size) {
    return;
  }
  size = Number(size);
  if (Number.isNaN(size)) {
    return;
  }
  css = "div.list-thumb{\n  width: " + size + "px;\n  height: " + size + "px;\n}";
  head = document.head || ((_ref = document.getElementsByTagName('head')) != null ? _ref[0] : void 0);
  style = document.createElement('style');
  style.type = 'text/css';
  if (style.styleSheet) {
    style.styleSheet.cssText = css;
  } else {
    style.appendChild(document.createTextNode(css));
  }
  return head.appendChild(style);
})();

window.addEventListener('load', function() {
  var buttons;
  buttons = document.querySelectorAll('.size-control button');
  buttons = [].slice.apply(buttons);
  return buttons.forEach(function(button) {
    return button.addEventListener('click', function() {
      localStorage['thumb-size'] = Number(button.innerHTML);
      return [].slice.apply(document.querySelectorAll('.list-thumb')).forEach(function(e) {
        e.style.width = localStorage['thumb-size'] + 'px';
        return e.style.height = localStorage['thumb-size'] + 'px';
      });
    });
  });
});
